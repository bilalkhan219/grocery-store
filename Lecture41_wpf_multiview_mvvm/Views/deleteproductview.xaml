<UserControl x:Class="wpfproject.Views.deleteproductview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfproject.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="DarkGray">
    <Grid>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock  Text="Enter Product ID to Delete " FontSize="25" FontFamily="Ariel" Margin="0,0,0,0"></TextBlock>
            <TextBox Text="{Binding IDdel, UpdateSourceTrigger=PropertyChanged}" Name="deleteidbox" Height="25" Width="250" VerticalContentAlignment="Bottom" Margin="0,30,0,0"></TextBox>
            <Button Command="{Binding delcmd}" Name="delbutton" Height="35" Width="120" Content="Delete" Margin="0,30,0,0"></Button>
        </StackPanel>
    </Grid>
</UserControl>
