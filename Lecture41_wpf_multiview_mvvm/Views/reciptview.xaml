<UserControl x:Class="wpfproject.Views.reciptview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfproject.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="DarkGray">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <DataGrid ItemsSource="{Binding productlist}" Width="500" Height="300"></DataGrid>
            <WrapPanel>
                <TextBlock Text="Total:" FontSize="25" Margin="0,20,0,0"></TextBlock>
                <TextBlock Text="{Binding totalcash}" FontSize="25" Margin="0,20,0,0"></TextBlock>
            </WrapPanel>
        </StackPanel>

    </Grid>
</UserControl>
